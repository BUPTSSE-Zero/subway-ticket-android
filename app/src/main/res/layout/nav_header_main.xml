<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_height"
    android:background="@color/primary"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/nav_header_top_padding"
    android:orientation="vertical"
    android:theme="@style/ThemeOverlay.AppCompat.Dark">

    <ImageView
        android:id="@+id/nav_header_portrait_main"
        android:layout_width="@dimen/nav_header_portrait_size"
        android:layout_height="@dimen/nav_header_portrait_size"
        android:src="@drawable/ic_account_circle_light_96dp"
        android:tint="@android:color/white"/>

    <TextView
        android:id="@+id/nav_header_userid_main"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/activity_vertical_margin"
        android:text="@string/click_avatar_to_login"
        android:textColor="@android:color/white"
        android:singleLine="true"/>

</LinearLayout>
